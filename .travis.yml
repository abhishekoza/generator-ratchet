language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ratchet' ]; then cd .. && eval "mv $currentfolder generator-ratchet" && cd generator-ratchet; fi

